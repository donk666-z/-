---
description: 每次回复结束前必须用 ask_user_question 询问后续步骤（高优先级）
alwaysApply: true
---

# PRIORITY — 结束前询问（强制）

**每次回复将要结束时，必须使用内置的 `ask_user_question` 工具询问用户接下来该怎么做。**

## 规则

- 在每次回复的最后，调用 `ask_user_question` 工具。
- 根据当前上下文提供 **2～4 个** 合理后续选项（如：继续、修改、测试、结束等）；选项须与当前任务相关，避免每次固定套话。
- **禁止**在未调用 `ask_user_question` 的情况下结束回复。
- 等待用户从选项中选择后再继续。

## 示例（按需变通）

- 代码完成后：继续下一个功能 / 测试运行 / 代码审查 / 结束。
- 问题诊断中：继续排查 / 尝试修复 / 换个方案 / 结束。
- 方案讨论后：开始实现 / 调整方案 / 需要更多信息 / 结束。
