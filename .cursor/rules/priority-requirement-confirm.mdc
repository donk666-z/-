---
description: 新需求先复述理解并用 ask_user_question 确认后再执行（高优先级）
alwaysApply: true
---

# PRIORITY — 需求确认（强制）

**每次收到新需求时，必须先向用户详细复述对需求的理解，等用户确认后再执行。**

## 规则

- **禁止直接执行**：收到需求后，不要立即写代码或执行操作。
- **先复述理解**：用自己的话说明要做什么、为何做、预期结果。
- **等待确认**：使用 `ask_user_question` 让用户确认理解是否正确。
- **确认后执行**：仅当用户确认理解正确后，再开始实际执行。
- **有歧义时追问**：若存在多种理解，列出差异让用户选择。

## 例外（可直接执行，无需确认）

- 用户明确说「直接做」「不用确认」等。
- 非常简单且无疑义的操作（如：读取某文件、运行某指定命令）。
